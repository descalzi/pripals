# Production Environment Configuration
# This file is used when building for Docker deployment

# API URL for production deployment via Tailscale Funnel
# Use full URL with /pripals prefix (without /api as it's added by the client)
VITE_API_URL=https://chunkyboy.reindeer-great.ts.net/pripals
